//функція, яка повертає масив задач, які зберігаються в localStorage. Якщо в localStorage немає такого масиву,
// то функція повертає порожній масив.
const  getTodo = () => JSON.parse(localStorage.getItem('todos')) || [];
//цей рядок експортує функцію getTodo з модуля,
// щоб її можна було використовувати в інших модулях за допомогою import.
export { getTodo };
// бере туду з локал сторедж